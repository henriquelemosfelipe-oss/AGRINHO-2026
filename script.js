const botao = document.getElementById("mensagemBtn");
const mensagem = document.getElementById("mensagem");

botao.addEventListener("click", () => {

    mensagem.innerHTML =
    "🌱 Cada árvore plantada, cada gota de água economizada e cada atitude consciente ajudam a construir um futuro melhor para todos!";
});
